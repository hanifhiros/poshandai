Handai POS — Codebase Review Package

Tujuan:
- Menyediakan paket kode yang aman untuk dikonsultasikan ke ahli.
- Menyorot modul penting dan area yang perlu diperiksa.

Isi paket:
- folder `app/` (kode aplikasi)
- folder `config/` (konfigurasi)
- folder `resources/` (views, assets)
- folder `routes/` (route definitions)
- folder `database/migrations/` (skema/riwayat migrasi)
- file `composer.json`, `package.json`, `README.md`, `MIGRATION_PLAN_NEXTJS.md`, `phpunit.xml`

Catatan keamanan:
- Saya sengaja mengecualikan `vendor/` dan file database (`database.sqlite*`) agar ukuran paket kecil dan tidak berisiko.
- Jika ingin audit dependency, sertakan `composer.lock` atau `vendor/` secara terpisah.

Area yang sebaiknya diperiksa oleh ahli:
- Inventory & stock flow: `app/Services/StockAlertService.php`, `app/Services/InventoryService.php`, `app/Models/Stock.php`, `app/Http/Controllers/Manager/Inventory/StockBatchController.php`
- Purchasing & supplier: `app/Http/Controllers/Manager/Operational/SupplierController.php`, import/export supplier (`app/Services/ImportExportService.php`)
- Production & MRP: `app/Services/ProductionPlanningService.php`, `app/Models/ProductionPlan.php`, produksi controller `app/Http/Controllers/Manager/Operational/ProductionPlanController.php`
- Waste & expired handling: `app/Http/Controllers/Manager/Operational/WasteController.php`, `app/Services/AccountingService.php` (expired/journal)
- Sales / POS integration: `app/Http/Controllers/POS/`, `app/Http/Controllers/Kasir/`
- Finance & reconciliation: `app/Http/Controllers/Manager/Finance/`, `app/Services/AccountingService.php`, `routes/web_manager.php`
- HR / Payroll groundwork: `app/Http/Controllers/Manager/Finance/EmployeeController.php`, `app/Models/Employee.php`, attendance `app/Http/Controllers/Manager/Operational/AttendanceController.php`

Checklist untuk ahli (singkat):
- Konsistensi event -> stock -> finance (one source of truth?)
- Missing workflows: PO/approval/receiving/QC/putaway, bank reconciliation automation
- Data integrity: supplier master, lead time, batch/lot handling, expiry tracking
- Payroll: calculation data sources, production-linked wages, attendance vs output
- Security & infra: DB location, backups, dependency vulnerabilities

Instruksi: setelah dicek, ahli bisa mengembalikan rekomendasi ke file `review_package/REVIEW_NOTES.md`.

---
Generated by code-review helper.